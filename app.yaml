# Last updated 11/26/2022

runtime: php81 # Replace with php74 to use PHP 7.4

handlers:
# Serve a directory as a static resource.
- url: /img/lobbies
  static_dir: lobbies

- url: /canvas
  static_dir: canvas
  
- url: /gallery
  static_dir: gallery
  
- url: /home
  static_dir: home

- url: /lobby
  static_dir: lobby

- url: /wheel
  static_dir: wheel

# Serve images as static resources.
# - url: /(.+\.(gif|png|jpg|ico))$
#   static_files: \1
#   upload: .+\.(gif|png|jpg|ico)$

# Serve your app through a front controller at 
# index.html, index.php or public/index.php.
- url: .*
  script: auto

entrypoint:
  serve handler.php

# environment variables
  # REPLACE USER, PASSWORD, DATABASE, AND CONNECTION_NAME with 
  # the values obtained when configuring your Cloud SQL instance
env_variables:
  INSTANCE_UNIX_SOCKET: /cloudsql/cs310-wheel-game:us-central1:cs310-wheel-game
  CLOUDSQL_USER: cs310-wheel-game 
  CLOUDSQL_PASSWORD: Z3kpapk:Cro;i+<Z
  CLOUDSQL_DB: wheel-db
# [END gae_cloudsql_mysql_env]




